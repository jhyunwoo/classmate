import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Rides() {
  return (
    <SafeAreaView>
      <Text className={"font-NanumEB text-4xl"}>Rides</Text>
    </SafeAreaView>
  );
}
