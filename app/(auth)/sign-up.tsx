import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SignUp() {
  return (
    <SafeAreaView>
      <Text className={"font-NanumEB text-4xl"}>Sign Up</Text>
    </SafeAreaView>
  );
}
